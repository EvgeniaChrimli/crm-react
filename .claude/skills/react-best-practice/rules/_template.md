---
title: TODO — short, specific title for this rule
impact: MEDIUM
tags: TODO, comma, separated
---

## TODO — same title as above

TODO: one or two sentences explaining the rule and, briefly, why it matters. Say
what to do, not just what to avoid.

**Incorrect (TODO — short label for what's wrong with this):**

```tsx
// TODO: a small, realistic snippet showing the mistake
```

**Correct (TODO — short label for why this fixes it):**

```tsx
// TODO: the same snippet, done the right way
```

Delete this template's TODOs and placeholder text once the rule is written — this
file is only a starting point, copy it to `rules/<prefix>-<short-description>.md`.
`npm run validate` requires both an "**Incorrect" and a "**Correct" example to be
present, and `title`/`impact` to be filled in (`impact` must be one of `CRITICAL`,
`HIGH`, `MEDIUM-HIGH`, `MEDIUM`, `LOW-MEDIUM`, `LOW`).
