{
  "name": "project-rules-skill",
  "version": "0.1.0",
  "private": true,
  "description": "Rule set for AI agents, built from rules/*.md into AGENTS.md.",
  "scripts": {
    "build": "node src/build.js",
    "validate": "node src/validate.js",
    "dev": "npm run build && npm run validate"
  }
}
